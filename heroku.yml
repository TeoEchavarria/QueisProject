build:
  config:
    RAILS_ENV: development
    FOO: bar
  docker:
    release:
       dockerfile: Dockerfile
       target: builder
    web: gunicorn app:app
    web: Dockerfile
run:
  web: bundle exec puma -C config/puma.rb