build:
  config:
    RAILS_ENV: development
    FOO: bar
  docker:
    release:
       dockerfile: Dockerfile
       target: builder
    web: Dockerfile
    web: uvicorn app:main
run:
  web: bundle exec puma -C config/puma.rb